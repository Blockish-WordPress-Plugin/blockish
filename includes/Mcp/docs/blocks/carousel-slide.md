### `blockish/carousel-slide`

See parent `blockish/carousel` docs.

**No Container** — background/layout live on the slide (`slideBackground`, flex, padding). Allowed content blocks only: heading, paragraph, button, image, icon, icon-list, rating, counter, social-icons.
